# A sample Gemfile
source "https://rubygems.org"

# gem "rails"

gem 'sinatra', '~> 2.0.4'
    gem 'rake'
    gem 'thin', '>= 1.8.0'
    gem 'shotgun'
    gem 'pry'
    gem 'require_all'

group :test do
      gem 'rspec'
      gem 'capybara', '>= 3.6.0'
      gem 'rack-test', '>= 2.0.0'
    end
